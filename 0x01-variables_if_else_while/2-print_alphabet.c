#include <stdio.h>
/**
 * main - Point of entry
 * Return: return 0 passed
 */
int main(void)
{
	char letter;

	letter = 'b';

	while
		(letter <= 'x') {
			putchar(letter);
			letter++;
		}
	putchar('\n');
	return (0);
}
