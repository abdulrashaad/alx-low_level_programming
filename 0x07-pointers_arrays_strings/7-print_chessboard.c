#include "main.h"

/**
 * print_chessboard - point of entry
 * @a: array
 * Return: return always 0 (passed)
 */

void print_chessboard(char [8])
{
	int i;

	int j;

	for (i = 0; i < 8; i++)
	{
		for (j = 0; j < 8; j++)

		_putchar('\n');
	}
}
